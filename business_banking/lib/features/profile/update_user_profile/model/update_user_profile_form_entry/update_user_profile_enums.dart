enum UserProfileUpdateResponseStatus { succeed, failed, unknown }
enum UserProfileUpdateUserFormInputStatus { valid, invalid, unknown }
enum UserProfileUpdateInputStatusType {
  userName,
  phoneNumber,
  address,
  email,
  unknown
}
